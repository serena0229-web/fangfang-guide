# 對話記錄：手機版漢堡選單功能開發

## 專案資訊
- **專案名稱**：芳芳姐理賠導航員網站
- **儲存庫**：https://github.com/serena0229-web/fangfang-guide
- **網站**：https://serena0229-web.github.io/fangfang-guide/
- **開發日期**：2026年2月11日

## 問題描述

使用者回報網站在手機上打開時，導航選單的選項看不到。在桌面版上可以正常顯示「關於我們」、「服務項目」、「客戶案例」等選項，但手機版這些選項完全隱藏了。

## 解決方案

為網站新增手機版漢堡選單功能，讓使用者在手機上可以透過點擊漢堡按鈕來展開/收起導航選單。

### 修改的文件

1. **index.html** - 主頁
2. **FAQ.html** - 常見問題頁

## 技術實作細節

### 1. CSS 樣式（新增於 `<style>` 標籤內）

#### 漢堡選單按鈕樣式
```css
/* 手機版漢堡選單按鈕 */
.menu-toggle {
    display: none;
    flex-direction: column;
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
    z-index: 1001;
}

.menu-toggle span {
    width: 25px;
    height: 3px;
    background-color: #3D405B;
    margin: 3px 0;
    transition: 0.3s;
    border-radius: 3px;
}

.menu-toggle.active span:nth-child(1) {
    transform: rotate(-45deg) translate(-5px, 6px);
}

.menu-toggle.active span:nth-child(2) {
    opacity: 0;
}

.menu-toggle.active span:nth-child(3) {
    transform: rotate(45deg) translate(-5px, -6px);
}
```

#### 響應式設計（768px 以下）
```css
@media (max-width: 768px) {
    .menu-toggle {
        display: flex;
        position: fixed;
        top: 20px;
        right: 20px;
    }

    .nav-links {
        position: fixed;
        top: 0;
        right: -100%;
        width: 70%;
        height: 100vh;
        background-color: rgba(248, 247, 244, 0.98);
        flex-direction: column;
        justify-content: flex-start;
        align-items: flex-start;
        padding: 80px 30px 30px;
        transition: right 0.3s ease;
        z-index: 1000;
        box-shadow: -2px 0 10px rgba(0, 0, 0, 0.1);
    }

    .nav-links.active {
        right: 0;
    }

    .nav-links a {
        width: 100%;
        padding: 15px 0;
        text-align: left;
        border-bottom: 1px solid #E0E1DD;
    }
}
```

### 2. HTML 結構（新增於 `</nav>` 之後）

```html
<!-- 手機版漢堡選單按鈕 -->
<button id="menu-toggle" class="menu-toggle" aria-label="選單">
    <span></span>
    <span></span>
    <span></span>
</button>
```

### 3. JavaScript 互動功能（新增於 `</body>` 之前）

```javascript
<!-- 手機版漢堁選單 JavaScript -->
<script>
    const menuToggle = document.getElementById('menu-toggle');
    const navLinks = document.querySelector('.nav-links');

    menuToggle.addEventListener('click', function() {
        navLinks.classList.toggle('active');
        menuToggle.classList.toggle('active');
    });
</script>
```

## 功能說明

1. **桌面版（>768px）**：
   - 漢堁選單按鈕隱藏
   - 導航選單水平顯示在頁面頂部

2. **手機版（≤768px）**：
   - 漢堁選單按鈕顯示在右上角
   - 點擊按鈕時，導航選單從右側滑出
   - 再次點擊可以收起選單
   - 按鈕有動畫效果（變成 X 形狀）

## 提交記錄

### Commit 1: index.html
- **訊息**：「完成手機版漢堁選單功能」
- **時間**：2026年2月11日
- **修改內容**：
  - 新增 CSS 樣式（約於第 935 行）
  - 新增 HTML 按鈕（約於第 1131 行）
  - 新增 JavaScript 代碼（約於第 1578 行）

### Commit 2: FAQ.html
- **訊息**：「完成 FAQ.html 手機版漢堁選單」
- **時間**：2026年2月11日
- **修改內容**：
  - 新增 HTML 按鈕（約於第 164 行之後）
  - 新增 JavaScript 代碼（約於第 368 行之前）
  - CSS 樣式已存在於原有檔案中

## 測試結果

✅ index.html - 功能正常
✅ FAQ.html - 功能正常
✅ 手機版導航選單可以正常展開/收起
✅ 桌面版顯示正常

## 相關連結

- GitHub 儲存庫：https://github.com/serena0229-web/fangfang-guide
- 線上網站：https://serena0229-web.github.io/fangfang-guide/
- FAQ 頁面：https://serena0229-web.github.io/fangfang-guide/FAQ.html
